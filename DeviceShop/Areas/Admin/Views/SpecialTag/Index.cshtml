@model IEnumerable<DeviceShop.Models.SpecialTag>
@{
    ViewData["Title"] = "Index";
}
<br />
<div style="padding-left:100px;width:80%">
    <div class="row">
        <div class="col-6">
            <h2 class=" text-info">Special Tag List</h2>
        </div>
        <div style="padding-left:285px;">
            <a asp-action="Create" class="btn btn-info"><i><i class="fas fa-plus"></i></i>&nbsp; New Tag</a>
        </div>
    </div>
    <br />
    <div class="p-4 border rounded ">
        <table id="tblData" class="table table-striped table-bordered" >
            <thead style="background-color:darkred">
                <tr class="table-info">
                    <th>Tag Name</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
</div>

@*<div>
    <table class="table table-striped border" style="width:50%">
        <tr class="table-info">
            <th>
                @Html.DisplayNameFor(c => c.ProductsType)
            </th>
            <th>
            </th>
            <th>
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ProductsType</td>
                <td>
                    <partial name="_ButtonPartial" model="@item.Id" />
                </td>
            </tr>
        }
    </table>
</div>*@

@section Scripts{

    <script src="~/js/specialTag.js"></script>
}